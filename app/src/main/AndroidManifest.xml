<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.projet4.maru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maréu">
        <activity
            android:name=".ui.meeting.RoomspinnerActivity"
            android:label="select Room" />
        <activity
            android:name=".ui.meeting.AddMeetingActivity"
            android:label="Ajout Meeting" />
        <activity
            android:name=".ui.meeting.SelectcoworkerActivity"
            android:label="Selection participant" />
        <activity
            android:name=".ui.meeting.DetailMeetingActivity"
            android:label="Détail Meeting"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.meeting.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>